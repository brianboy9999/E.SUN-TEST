# Backend Skeleton (Spring Boot)

This folder is a scaffold only.

Planned stack:
- Spring Boot
- Spring JDBC
- SQLite
- Pure SQL in repository layer

Suggested next steps later:
1. Generate Spring Boot project here.
2. Add SQLite JDBC dependency.
3. Place SQL files in `src/main/resources/sql`.
4. Implement repository classes under `src/main/java/repository`.
